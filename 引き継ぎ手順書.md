# TT月間スケジュール 引き継ぎ手順書

このアプリを別のパソコンに引き継ぎ、今後そちらから修正・指示を出すための手順書です。

---

## 1. このアプリの基本情報

| 項目 | 内容 |
|---|---|
| アプリ名 | TT月間スケジュール（社内スケジュール予定表） |
| 公開URL（みんなが見る画面） | https://tohoku-tent.github.io/schedule/schedule.html |
| ソースの保管場所（GitHub） | https://github.com/tohoku-tent/schedule |
| GitHubアカウント | tohoku-tent（メール: kaneko@tohoku-tent.jp） |
| データの保存先 | Firebase（tohoku-tent-schedule プロジェクト） |

### ★ 一番大事なこと
- **プログラムの中身は、すべてGitHub（上記URL）に保存されています。**
- **予定・写真・連絡事項などのデータは、Firebase（クラウド）に保存されています。**
- どちらも「クラウド上」にあるので、**パソコン本体には何も残っていなくても大丈夫**です。
  新しいパソコンでGitHubから取り込めば、これまでの続きから作業できます。

---

## 2. ファイル構成（GitHubに入っているもの）

| ファイル | 役割 |
|---|---|
| `schedule.html` | **カレンダー本体**（これがアプリのすべて） |
| `sw.js` | 自動更新の仕組み（サービスワーカー） |
| `manifest.json` | ホーム画面追加用の設定 |
| `icons/icon-192.png` `icons/icon-512.png` | アプリのアイコン |
| `README.md` | 説明ファイル |
| `引き継ぎ手順書.md` | このファイル |

---

## 3. 新しいパソコンでの準備（最初の1回だけ）

### ① Claude Code をインストール
今使っているのと同じ「Claude Code」を新しいパソコンにインストールし、ログインします。

### ② Git をインストール
https://git-scm.com/ からダウンロードしてインストールします。

### ③ GitHubからソースを取り込む（クローン）
Claude Codeに、以下をお願いすればOKです（自分でやる場合のコマンド）：

```
git clone https://github.com/tohoku-tent/schedule.git
```

これで `schedule` フォルダができ、中に全ファイルが入ります。

### ④ 初回の保存（プッシュ）時にGitHubログインを求められます
修正後に初めてGitHubへ反映するとき、GitHubのログイン画面が出ます。
**tohoku-tent アカウント**でログインしてください（1回ログインすれば以降は自動）。

---

## 4. 今後の作業の流れ（新しいパソコン）

これまでは「photo-days フォルダで編集 → schedule フォルダにコピー」という2段階でしたが、
**新しいパソコンでは、クローンした `schedule` フォルダだけで作業すればシンプルです。**

1. Claude Codeを開き、`schedule` フォルダで作業を指示する
2. Claudeが `schedule.html` などを修正
3. GitHubへ反映（`git push`）
4. 数分後、公開URLに自動で反映される

指示の出し方は今まで通り、日本語で「〇〇を直して」「〇〇を追加して」でOKです。

---

## 5. 引き継ぎ後、この（古い）パソコンでやること
- 特に何もしなくても大丈夫です（データはクラウドにあるため）。
- 混乱を避けたい場合は、古いパソコンのフォルダはそのまま触らないでください。
  （両方のパソコンから同時に修正すると、GitHubで食い違いが起きることがあります）

---

## 6. 困ったときのメモ
- **公開画面が古いまま**：ページを再読み込み（iPhoneはタブを閉じて開き直す）。
  自動更新の仕組みが入っているので、基本は数分で最新になります。
- **データが見えない**：インターネット接続を確認（Firebaseに接続できないと表示されません）。
- **修正がうまく反映されない**：GitHubへ `git push` できているか、Claude Codeに確認してもらってください。

---

作成日: 2026-07-21
